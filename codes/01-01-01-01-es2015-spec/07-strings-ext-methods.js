// 字符串的扩展方法

const message = 'Error: foo is not defined.'

console.log(
  // 开头的方法
  // message.startsWith('Error')
  // 结束的方法
  // message.endsWith('.')
  // 是否包含 
  message.includes('foo')
)
